import { Routes } from '@angular/router';
import { ConfigPageComponent } from './pages/config/config.component';

export const CONFIG_ROUTES: Routes = [
  { path: '', component: ConfigPageComponent }
];
